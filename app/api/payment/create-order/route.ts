import {NextResponse} from "next/server";
export async function POST(req:Request){
 try{const {amount}=await req.json();const key=process.env.RAZORPAY_KEY_ID;const secret=process.env.RAZORPAY_KEY_SECRET;if(!key||!secret)return NextResponse.json({error:"Razorpay test keys are not configured."},{status:500});const paise=Math.round(Number(amount)*100);if(!Number.isFinite(paise)||paise<100)return NextResponse.json({error:"Invalid payment amount."},{status:400});const auth=Buffer.from(`${key}:${secret}`).toString("base64");const r=await fetch("https://api.razorpay.com/v1/orders",{method:"POST",headers:{Authorization:`Basic ${auth}`,"Content-Type":"application/json"},body:JSON.stringify({amount:paise,currency:"INR",receipt:`pg_${Date.now()}`,payment_capture:1})});const data=await r.json();if(!r.ok)return NextResponse.json({error:data.error?.description||"Razorpay order creation failed."},{status:r.status});return NextResponse.json({orderId:data.id,amount:data.amount,key});}catch{return NextResponse.json({error:"Unable to create payment order."},{status:500})}}
